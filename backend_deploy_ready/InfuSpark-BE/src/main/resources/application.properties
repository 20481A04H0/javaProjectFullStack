spring.application.name=IQQuest_Learning-BE
server.port=${PORT:8080}
management.endpoints.web.exposure.include=info
management.info.env.enabled=true
spring.main.banner-mode=off
spring.jpa.open-in-view=false

# Active Profile - Use prod for PostgreSQL
spring.profiles.active=${SPRING_PROFILES_ACTIVE:prod}

# Database Configuration - PostgreSQL Cloud Database
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://dpg-d2rjhmffte5s738bm0p0-a.oregon-postgres.render.com:5432/lohithraj001}
spring.datasource.username=${DB_USERNAME:lohithraj001_user}
spring.datasource.password=${DB_PASSWORD:s5Tl9PgPRxzsugEKOE8QsJtN5CH1hfWV}
spring.datasource.driver-class-name=${DB_DRIVER:org.postgresql.Driver}

# JPA Configuration for PostgreSQL
spring.jpa.database-platform=${DB_DIALECT:org.hibernate.dialect.PostgreSQLDialect}
spring.jpa.hibernate.ddl-auto=update
spring.sql.init.mode=always
spring.jpa.defer-datasource-initialization=true
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=${DB_DIALECT:org.hibernate.dialect.PostgreSQLDialect}
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# H2 Console (for development only)
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

spring.main.allow-bean-definition-overriding=true

spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# CORS Configuration
spring.web.cors.allowed-origins=${ALLOWED_ORIGINS:*}
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*
spring.web.cors.allow-credentials=true